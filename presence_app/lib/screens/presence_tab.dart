import 'package:flutter/material.dart';

class PresenceTab extends StatelessWidget {
  const PresenceTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Check your Presence Status'),
    );
  }
}

